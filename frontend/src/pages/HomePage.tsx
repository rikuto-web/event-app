import type { Component } from "solid-js";

export const HomePage: Component = () => {
  return (
    <section>
      <h1 class="text-2xl font-semibold">イベント一覧</h1>
      <p class="mt-2 text-gray-600">イベントはまだありません。VS-02 で一覧 UI を実装します。</p>
    </section>
  );
};
