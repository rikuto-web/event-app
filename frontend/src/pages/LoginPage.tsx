import type { Component } from "solid-js";

export const LoginPage: Component = () => {
  return (
    <section class="mx-auto max-w-md rounded-lg border border-gray-200 bg-white p-8 shadow-sm">
      <h1 class="text-2xl font-semibold">ログイン</h1>
      <p class="mt-2 text-gray-600">VS-01 で認証フォームを実装します。</p>
    </section>
  );
};
