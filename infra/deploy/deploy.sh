#!/usr/bin/env bash
# VS-11 以降: OCIR push + VM 上 compose up（骨格のみ）
set -euo pipefail

echo "deploy.sh is a placeholder until VS-11 (Docker Compose + Dockerfiles) is complete."
echo "Infra acquisition (terraform apply) can proceed without this script."
exit 0
